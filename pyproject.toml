[build-system]
requires = [
  "setuptools>=69",
  "wheel",
  "packaging",
  "numpy>=2,<3",
  "scipy",
  "Cython>=3",
  "charset_normalizer",
  "meson",
  "ninja"
]
build-backend = "setuptools.build_meta"
